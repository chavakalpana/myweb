pipeline{
			agent any
			environment{
			    PATH="/opt/maven/bin:$PATH"
                        }
			  stages 
			{
					stage('Build') 
					{
					steps{
					def -f /var/lib/jenkins/workspace/pipeline \n /opt/maven/bin/mvn clean
					sh "mvn clean"
					}
					}
					stage('Test') 
					{
					steps{
					sh "mvn test"
					}
					}
					stage('Compile') 
					{
					steps{
					sh "mvn compile"
					}
					}
				}
				
}
